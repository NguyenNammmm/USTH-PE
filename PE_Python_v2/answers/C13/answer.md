# C13 - Khám phá API, traceback và module chuẩn

Dành cho giảng viên/dev; không gửi trước vào client challenge.

```python
import statistics
def mean_or_none(values):
    return statistics.mean(values) if values else None
```

## Đáp án kiểm tra hiểu

1. Muốn tính trung bình bằng thư viện chuẩn, chọn module nào?

Đáp án: statistics

- statistics: Đúng: statistics có mean.
- tkinter: tkinter tạo giao diện.
- random: random lấy mẫu ngẫu nhiên.

